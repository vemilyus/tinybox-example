pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name='tinybox'

include 'tinybox-auth',
        'tinybox-storage',
        'tinybox-sharing',
        'tinybox-gateway'
